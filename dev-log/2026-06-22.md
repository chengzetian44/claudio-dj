---
date: 2026-06-22
session: 全面代码审查 + 12 项修复
---

## 第一轮 — 高优先级修复（5 项）

### 1. xml2js 崩溃隐患
- **文件**: VOICE/upnp.js
- **问题**: `require('xml2js')` 未安装且未使用，加载即崩
- **修复**: 删除该行

### 2. PWA 能力补全
- **文件**: 新增 PWA/manifest.json, PWA/sw.js, PWA/assets/icon.svg；修改 PWA/index.html
- **修复**: manifest + Service Worker（缓存策略：静态资源 cache-first、API network-only、HTML network-first）+ SVG 图标

### 3. Toast 通知 + 消灭空 catch
- **文件**: 新增 PWA/js/toast.js；修改 player.js, chat.js, app.js, style.css, index.html
- **问题**: 前端 16 处 `.catch(() => {})` 静默吞掉所有错误
- **修复**: 新增 Toast 组件，11 处关键 catch 添加用户可见提示

### 4. 加载状态全覆盖
- **文件**: chat.js, player.js, style.css
- **修复**: 聊天打字指示器 "Claudio 正在思考 ●●●" + 封面加载脉冲动画

### 5. 移动端聊天面板开关
- **文件**: index.html, style.css, app.js
- **问题**: 响应式布局隐藏聊天面板但无打开方式
- **修复**: 侧边栏聊天气泡按钮 + 关闭按钮 + 半透明遮罩层

## 第二轮 — 中优先级修复（6 项 + 1）

### 6. prev() 改为真正的上一首
- **文件**: player.js
- **修复**: 20 首播放历史栈，prev() 弹出并加载历史歌曲

### 7. STATE.DB 索引优化
- **文件**: STATE.DB.js
- **修复**: 4 个 CREATE INDEX IF NOT EXISTS（messages.session_id, play_history.played_at, likes.liked_at, skips.skipped_at）

### 8. 意图分类修复
- **文件**: ROUTER.JS
- **问题**: "我想听周杰伦" 被分类为 CHAT
- **修复**: 添加 `我想听`/`想听`/`来首` 到 PLAY 模式；裸 `听` 触发 PLAY 但排除 `听的` 从句

### 9. Media Session API 集成
- **文件**: player.js
- **修复**: 锁屏/通知栏显示歌曲信息 + 播放/暂停/上一首/下一首控制

### 10. 粒子动画暂停优化
- **文件**: particles.js
- **修复**: visibilitychange 监听，切标签页时 cancelAnimationFrame

### 11. ROUTER.JS 重复代码提取
- **文件**: ROUTER.JS
- **修复**: 提取 readTasteHint() 共享函数；4 处 handler 统一使用 resolveTrack()

### 12. 连续播放修复 (getNextTrack)
- **文件**: ROUTER.JS
- **问题**: 重构引入二次搜索 bug + 无去重导致重复推荐
- **修复**: 恢复 getTrackUrl 直连 + recentlyPlayed 去重

## 待办

- 中优先级未处理: AI 流式响应、天气城市配置
- 低优先级未处理: 键盘快捷键、歌单管理、TTS 缓存清理、UPnP SOAP、飞书集成

---

## 第四轮 — TTS 语音系统改造（代码就绪，GPT-SoVITS 待安装）

### 16. TTS 重写：Fish Audio → GPT-SoVITS 本地声音克隆

- **文件**: VOICE/tts.js, .env, .env.example
- **问题**: Fish Audio API (`api.fish.audio`) 在国内被屏蔽，服务端 TTS 完全不可用。定时播报（早间简报、整点提醒）只有文字没有语音。
- **目标方案**: 使用 GPT-SoVITS 开源声音克隆工具，本地训练身边人的声音作为 DJ 音色。

**已完成（代码侧）**：
- `VOICE/tts.js` 完全重写：HTTP 调用本地 GPT-SoVITS API (`http://127.0.0.1:9880/tts`)，替代原来的 Fish Audio HTTPS 调用
- 保持 MD5 缓存机制（text + model → hash → `tts/xxx.wav`）
- 优雅降级：GPT-SoVITS 未启动时返回 `null`，不崩溃
- `.env` 配置更新：`TTS_API_URL`、`TTS_REF_AUDIO`、`TTS_PROMPT_TEXT`、`TTS_PROMPT_LANG`
- 移除 Fish Audio 相关配置（`FISH_AUDIO_KEY`）

**待完成（需手动操作）**：
1. 下载 GPT-SoVITS Windows 整合包（约 10GB，含基础模型）
2. 解压 + 启动 API：`python api_v2.py`
3. 配置 `.env` 中参考音频路径
4. 后续：录制身边人声音 → 训练专属模型（GPU 约 30 分钟）

### 环境信息
- Python 3.11.9 ✅
- NVIDIA GPU + CUDA 13.0 ✅
- GPT-SoVITS: 待安装

---

## 第三轮 — 推荐→播放链路修复（3 项）

### 13. 推荐后自动播放
- **文件**: ROUTER.JS, chat.js, player.js
- **问题**: 推荐返回 tracks 数组但 frontend 只检查 data.track（单首）；HTTP 回退忽略 recommend action；speakThenPlay 等 TTS 结束才播
- **修复**:
  - 后端 recommend 路径解析第一首 URL 放入 data.track
  - 前端 HTTP 回退也处理 recommend action
  - 音乐 0.8 秒后直接播放不等 TTS 结束

### 14. 多首推荐 + 队列连续播放
- **文件**: ROUTER.JS, player.js, chat.js, prompts/dj-persona.md
- **问题**: 推荐只返回一首，播完就停
- **修复**:
  - 后端 3 处推荐逻辑改为批量解析 URL（Promise.all）+ 新增 searchWithFallback() 降级搜索
  - Player 新增 queue[] + enqueue()；next() 改为队列优先
  - chat.js speakThenPlay + HTTP 回退：收到 tracks 数组 → 入队
  - AI Prompt：教 AI 用简短搜索关键词

### 15. 推荐歌曲与播放歌曲一致
- **文件**: prompts/dj-persona.md, BRAIN/claude.js, ROUTER.JS, chat.js
- **问题**: AI 在文字中提歌名，但 ♪recommend:风格 搜索返回不同歌曲 → 错位
- **修复**:
  - BRAIN parseResponse 支持多条 ♪play: matchAll → trackNames 数组
  - ROUTER 两处 play 处理：多条 trackNames → Promise.all 解析全部 → data.tracks
  - AI Prompt：点名推荐用多条 ♪play:（说的歌 = 放的歌）；风格推荐用 ♪recommend:（不提歌名）
  - 前端 chat.js showTrackList()：推荐时聊天回显 🎵 即将播放：歌单列表

---

## 第四轮 — TTS 语音系统改造（2 项）

### 16. Edge TTS 替代 Fish Audio
- **文件**: VOICE/tts.js, .env, .env.example
- **问题**: Fish Audio API 在国内被屏蔽，服务端 TTS 完全不可用。定时播报只有文字无语音。
- **修复**:
  - VOICE/tts.js 完全重写：调用 Python edge-tts CLI（微软免费神经网络语音）
  - MD5 缓存机制保留（text + voice → hash → `tts/xxx.mp3`）
  - 音色：zh-CN-YunxiNeural（云希男声，温暖沉稳）
  - pip install edge-tts 部署到 Railway

### 17. AI 聊天接入 TTS 语音
- **文件**: ROUTER.JS, chat.js
- **问题**: AI 聊天回复没有语音，手机上完全无声
- **修复**:
  - ROUTER handleWs → 发文字 reply → 后台 synthesizeAndSend → 合成完毕推送 tts.play
  - 前端 chat.js speakThenPlay：收到 audioUrl 用 Audio 元素播放（兼容手机）
  - schedule.alert / tts.play 消息携带 audioUrl

---

## 第五轮 — 部署 Railway（3 项）

### 18. 数据库迁移：better-sqlite3 → sql.js
- **文件**: STATE.DB.js, package.json
- **问题**: better-sqlite3 需要 Python 编译原生模块，Railway 构建一直失败（node-gyp 找不到 Python）
- **修复**:
  - 替换为 sql.js（纯 JavaScript SQLite 实现，无需编译）
  - 重写 STATE.DB.js：init 改为 async，run/get/all 适配 sql.js API
  - 每次写操作后自动 save() 持久化
  - 所有调用方已兼容

### 19. Railway Docker + GitHub 部署
- **文件**: 新增 Dockerfile, nixpacks.toml, Procfile, railway.toml
- **结果**: 成功部署到 https://amiable-stillness-production.up.railway.app
- **配置**: 环境变量 ANTHROPIC_AUTH_TOKEN / BASE_URL / MODEL / TTS_VOICE

### 20. 多音色系统
- **文件**: VOICE/tts.js, SCHEDULER.JS, ROUTER.JS, .env, PWA/index.html, app.js, chat.js
- **问题**: 只有一种音色，所有场景用同一个声音
- **修复**:
  - VOICE/tts.js synthesize(text, voice?) 支持可选音色参数
  - 场景音色配置：TTS_VOICE_CHAT/BRIEFING/REMINDER/WEATHER/NIGHT
  - SCHEDULER 定时任务各场景指定不同音色
  - ROUTER AI 聊天使用用户选择的音色
  - 前端设置页新增 Edge TTS 音色下拉（8 种选择）：云希/云健/云扬/云枫/云皓/晓晓/晓伊/晓辰
  - localStorage 保存偏好 → WebSocket 聊天时携带音色参数

---

## 第六轮 — 移动端 + 网络穿透（2 项）

### 21. 局域网 + 公网访问
- **文件**: server.js
- **问题**: 手机无法访问电台
- **修复**:
  - server.js HOST 改为 0.0.0.0（允许局域网连接）
  - 手机同 WiFi → http://192.168.3.7:8080
  - localtunnel 内网穿透 → https://early-files-heal.loca.lt（任何网络）

### 22. PWA 移动端优化
- **文件**: chat.js, app.js, style.css, index.html
- **问题**: 手机端聊天面板无法打开、无语音
- **修复**:
  - 移动端聊天气泡按钮 + 关闭按钮 + 遮罩层
  - 服务端 TTS 音频 URL → 前端 Audio 元素播放（手机 speechSynthesis 不可用）

---

## 待办

- 中优先级: AI 流式响应 (chat.chunk)
- 低优先级: 键盘快捷键、歌单管理、TTS 缓存清理、GPT-SoVITS 声音克隆

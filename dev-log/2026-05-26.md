# 开发日志 — 2026-05-26

## 今日完成

### 1. AI 诊断与播放链路修复

- [x] 排查 "Claudio 正在休息中" 问题：服务器进程丢失 `ANTHROPIC_AUTH_TOKEN` 环境变量，重置后恢复
- [x] 修复 `ROUTER.JS` `handleAIChat` null 返回值崩溃 → 空值检查 + 错误日志
- [x] 修复 `STATE.DB` 数据库文件与 JS 模块同名冲突 → 数据库文件改名为 `claudio.db`，require 加 `.js` 后缀，`init()` 加幂等保护
- [x] 根因分析播放失败：`resolveUrl()` 调用 `music.163.com/song/media/outer/url` 返回重定向，浏览器 `<audio>` 不可直接播放
- [x] 重写 `player.js`：移除过时 `resolveUrl`，直接用服务器返回的 CDN 直链；AI 推荐无 URL 时自动调 `/api/resolve`
- [x] 新增 `GET /api/resolve?keyword=` 端点 → 搜索 + URL 解析一体
- [x] 新增 `GET /api/lyric?id=` 端点 → LRC 歌词

### 2. 搜索准确率修复（重大）

- [x] 根因：`cloudsearch` 返回翻唱/混音排在原唱之前（"稻香" → Lucky小爱 翻唱，非周杰伦）
- [x] 新增智能排名 `rankTracks()`：艺人匹配 +20、原唱 +5~8、翻唱标记 -15、括号修饰 -5
- [x] 新增艺人名清洗 `cleanArtistName()`：去尾部 `-` `.` `/Remixer` 后缀
- [x] 新增艺人过滤 `filterArtists()`：中文艺人存在时过滤纯拉丁名混音师

### 3. Phase 7：调度系统

- [x] 安装 node-cron
- [x] `SCHEDULER.JS` 三个定时任务：
  - 07:00 早间计划（今日日程 + 晨间音乐推荐）
  - 09:00 早间简报（AI 生成 + 真实天气 + 计划提醒）
  - 每小时检查（10/14/16 时待办提醒，23-07 静默）
- [x] 时区 Asia/Shanghai
- [x] 前端 `chat.js` 处理 `schedule.alert` 消息类型

### 4. Phase 8：语音与环境

- [x] `VOICE/weather.js` — wttr.in 免费天气（无 Key），OpenWeatherMap 可选（有 Key 时优先）
- [x] `VOICE/tts.js` — Fish Audio API + MD5 文件缓存 `/tts/`
- [x] `VOICE/upnp.js` — SSDP 设备发现 + AVTransport 框架
- [x] 调度器 `speakAndBroadcast()` 集成 TTS + Weather
- [x] `.env` 文件配置 `FISH_AUDIO_KEY`、`OWM_API_KEY`、`WEATHER_CITY`
- [x] Fish Audio API 域名 `api.fish.audio` 被国内网络屏蔽 → 切换为浏览器 Web Speech API
- [x] TTS 语音播报流程：AI 回复 → 浏览器朗读 → 播完自动放歌
- [x] 设置页新增语音音色选择器（自动列出 Windows 已安装的中文语音）
- [x] 音色试听按钮：点击朗读 "你好，我是Claudio..."

### 5. Phase 9：收尾完善

- [x] 歌词显示：LRC 解析 + 播放进度实时高亮 + 自动滚动
- [x] 清理临时文件 `restart-and-test.js`
- [x] 更新 `docs/implementation-steps.md`（9 阶段全部标记完成）
- [x] 更新 `.env.example`（完整配置项说明）
- [x] 创建 Windows 启动脚本 `start.bat`

### 6. AI 对话质量优化（下午）

- [x] 问题诊断：max_tokens 仅 1024 → 提升至 **4096**
- [x] 问题诊断：强制 JSON 输出使 AI 变机器人 → 改为**自然语言 + 轻量标记**
- [x] 问题诊断：DJ 人设限制"不超过 3 句话" → 移除限制
- [x] 重写 `prompts/dj-persona.md`：更自然的 DJ 人格、音乐知识、场景感知
- [x] 重写 `BRAIN/claude.js` `parseResponse()`：解析 `♪play:歌名:歌手名` / `♪recommend:风格` / `♪schedule:事项`
- [x] 更新 `CONEXIT.HUDOW/assembler.js`：移除强制 JSON 指令
- [x] 修复意图分类：RECOMMEND/QUERY 并入 AI 通道，不再用本地公式回复
- [x] 验证效果：回复从一句话变成有深度的 DJ 对话（聊 Nujabes、采样艺术、场景氛围）

### 7. 记忆系统（傍晚）

- [x] `STATE.DB` 新增 `likes` 和 `skips` 两张表
- [x] 新增 API：`POST /api/like` / `POST /api/skip` / `GET /api/stats`
- [x] 播放器 UI 新增 ♡ 喜欢按钮（点红心）和 ✕ 跳过按钮（跳下一首）
- [x] `player.js` 实现 like/skip 前端逻辑 + API 调用
- [x] 个人资料页展示累计播放次数和今日播放数（不再显示 `--`）
- [x] `CONEXIT.HUDOW/assembler.js` 注入最近 10 条 likes/skips 到 System Prompt
- [x] AI 能记住用户口味：既读品味档案，也看行为记录

---

## 项目文件清单（38 个）

```
BRAIN/claude.js           — AI 大脑（SDK + CLI 双通道，自然语言解析）
CLAUDE.md                 — AI 工作指引
CONEXIT.HUDOW/assembler.js — Prompt 组装（含记忆注入）
MUSIC/netease.js          — 网易云 API（智能排名 + 艺人清洗）
ROUTER.JS                 — 意图路由（仅播放走本地，其他走 AI）
SCHEDULER.JS              — 定时调度（cron + TTS + 天气）
STATE.DB.js               — SQLite（7 张表）
TTS.JS                    — TTS 入口
server.js                 — Express 主入口（8 API + WebSocket）
start.bat                 — Windows 启动脚本
package.json / .gitignore / .env / .env.example

USER/taste.md / routines.md / playlists.json / good-rules.md
prompts/dj-persona.md

VOICE/tts.js / weather.js / upnp.js

PWA/index.html / css/style.css
PWA/js/particles.js / player.js / chat.js / app.js

docs/requirements.md / tech-spec.md / implementation-steps.md / architecture.md
dev-log/2026-05-21.md / 2026-05-26.md / template.md
```

---

## 待办

- [ ] UPnP SOAP 完整播放控制
- [ ] PWA Service Worker 离线缓存
- [ ] 飞书日程集成
- [ ] 更多 Windows 语音包安装（增强 TTS 体验）
